<h1><%= @kitchen.name %></h1>
<% if @reviews %>
  <% @reviews.each do |review|%>
    <p><%= review.user.first_name %>: <%= review.body %></p>
      <%= link_to "delete", kitchen_review_path(@kitchen, review), method: :delete, data: {confirm: "Are you sure?"} %>
  <% end %>
<% end %>
